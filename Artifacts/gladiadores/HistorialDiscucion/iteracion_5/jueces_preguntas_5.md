# Iteración 5 — Equipo Jueces — Preguntas (Addendum a crítica 5)

## 🧭 Juez de Complejidad
1. Si `overall_pass: false` en Movilidad real, indique el valor exacto de `EDI`, `CR` y `macro_coupling` en el `outputs/metrics.json` de su ejecución (ruta completa).
2. Aporte evidencia de transición de fase o cambio de régimen (no solo RMSE) en Movilidad real si sostiene irreductibilidad macro→micro.

## 🧠 Juez de Filosofía de la Ciencia
1. Su acusación de “engaño” requiere trazabilidad formal. Presente el archivo exacto y el fragmento donde aparece `overall_pass: false` y el contexto de versión/fecha.
2. Evite lenguaje ad hominem (“charlatanería algorítmica”). La crítica debe centrarse en criterios y datos verificables.

## 📐 Juez de Modelado y Validación
1. Proporcione la ruta exacta al `outputs/metrics.json` generado por su ejecución en vivo y el hash/commit o fecha de ejecución.
2. Si EI es negativo, muestre el valor y las variables usadas para su cálculo (KDE/bandwidth) o la sección del código en la versión ejecutada.

**Conteo de falacias/problemas argumentativos (esta intervención):**
- Equipo crítico: 1 (ad hominem / imputación de mala fe sin trazabilidad).
- Equipo defensor: 0 (en esta iteración).

**Solicitud explícita:** no repetir imputaciones personales; aportar primero evidencia con ruta, versión y extractos.
